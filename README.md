# University Outcomes & Alumni Advantage Tracker

How well universities convert resources into alumni success — modeled after the [S&P 500 Executive Compensation Tracker](https://rayhe.github.io/sp500-exec-comp).

**Live:** https://rayhe.github.io/university-outcomes-tracker  
**Data:** 249 universities v0.34 (249/249 Scorecard-real 100%, 249/249 endowment-real, 235/249 research real NSF HERD, alumni_giving + employment_6mo 249/249 honestly labeled synthetic placeholders with per-record _source fields after the 2026-09-26 feasibility probe found no public API for either, 250/250 lat/long with source labels, 249/249 distinct IDs, 45 conferences, 43 liberal arts colleges, 21 HBCUs tagged (SWAC/MEAC/SIAC + Tennessee State + Hampton, per-record _hbcu_source) with cohort filter + ?cohort= deep-link + dynamic HBCU/LAC comparison insight cards, score formula declamped 55-97, correlations carry two-tailed p-values, sort state deep-links via ?sort=, dimension card is a true radial radar with min–max 0–100 normalization, peer network adds amber Carnegie crosswalk edges, drag persistence with localStorage, and score-weighted peer links (distance/strength ∝ score proximity + peer_group), compare table exports CSV with raw values, peer benchmarking table exports benchmark CSV with raw per-group values, peer links gain hover tooltips (pair/group/score &#916;/strength), geographic map draws score-weighted peer-group links (2 score-nearest per campus, width = strength, hover for the pair, toggleable), v0.31 adds 20 HBCUs + 29 liberal arts colleges — all Scorecard-enriched, existing 200 scores preserved, 1 duplicate deduped, v0.32 closes the new-49 placeholder debt: 49 endowments verified (Wikipedia infoboxes + 3 primary-source values), 37/49 research matched to NSF HERD FY2024)
**Update cadence:** Hourly until 9.0+ quality, then daily

## What It Measures

Composite **Alumni Advantage Score (0-100)** = weighted sum of:

1. **Career Outcomes (30%)** — median earnings 10yr (College Scorecard), employment 6mo, grad school rate, Fortune 500 alumni %, startup founders per 1k
2. **Alumni Support & Network (20%)** — alumni giving rate, endowment per student (NACUBO/IPEDS), network size, career services score
3. **Academic Quality (15%)** — 6yr grad rate, retention, student-faculty ratio, research spend per student (NSF HERD)
4. **Financial Health (15%)** — endowment 5yr growth, tuition reliance (990/audited), state appropriation stability, credit rating
5. **Value & ROI (20%)** — net price low-income, Pell gap, loan default rate, debt at graduation, 10yr ROI

6. **Public Filings (trust signal, unweighted)** — IPEDS, IRS 990, audited financials, College Scorecard, NSF HERD, state audit

## Public Filings Sources

| Source | What | Where |
|---|---|---|
| **IPEDS** | Enrollment, grad rate, retention, finance, SF ratio, state appropriation | NCES IPEDS Use-the-Data API |
| **IRS 990** | Revenue, expenses, endowment Schedule D, exec comp | ProPublica Nonprofit Explorer / IRS 990 XML |
| **Audited Financials** | GAAP financials, endowment footnotes | University controller sites |
| **College Scorecard** | Earnings, debt, default, net price, Pell | data.ed.gov Scorecard API |
| **NSF HERD** | Research expenditures | NSF HERD survey |
| **State Audit** | Public university single audit | State auditor |

Filing presence is a **trust signal**, not a score weight — similar to how DEF 14A completeness signals data quality in the S&P tracker.

## Roadmap

- v0.1 (now): 60 universities, synthetic enrichment, full UI, methodology
- v0.2 (hour +1): Replace synthetic earnings with College Scorecard API batch, add direct IPEDS Finance API
- v0.3 (hour +2): IRS 990 XML enrichment via ProPublica, NACUBO endowment table scrape, HERD Excel parser
- v0.4: Add 140 more universities (200 total), peer network (conference / Carnegie / geography), trends — **DONE v0.7 200/200 real**
- v0.12 (2026-09-04): **DONE** — endowment_b now NACUBO 2025 NCSE FY2025 real for 114/200 (explicit hand-adjudicated allowlist; system/foundation figures flagged via `_endowment_scope`; raw fetch artifacts in `data/raw/wikipedia-endowment/`)
- v0.14 (2026-09-06): **DONE** — endowment gap fill + absurd-value correction: 10 NACUBO-table assignments for v0.13-corrected identities (uva 11.23, georgia 2.18, kentucky 2.17, rochester 3.24, alabama 2.59 UA System scoped, oregonstate 1.01, southcarolina 1.15, auburn 1.31, udel 2.06 restored after delaware2 replacement orphaned it, umassamherst 1.80 system-scoped) + 8 absurd legacy synthetics replaced with publicly-verified values (usd 12.0→0.77 Wikipedia 2025, spelman 10.5→0.61 audited FY2025, morehouse 9.3→0.28 AP 2024, chapman 7.6→0.86 president address, northeastern 7.5→2.1 Wikipedia 2025, lmu 5.1→0.72 Wikipedia 2024 Loyola Marymount, ucd 4.1→2.2 UC Davis newsletter, byu 2.1→3.71 Wikipedia FY2025); 131/200 endowment real (was 113/200), remaining 69 explicitly labeled `synthetic (placeholder)`; raw sources in `data/raw/endowment-manual/2026-09-06/` (UCSB + UW-Seattle unverifiable this run — two search fetches failed, left synthetic)
- v0.15 (2026-09-07): **DONE** — verified endowment gap fill: 9 synthetic placeholders replaced with primary-source values, each verified against a document committed under `data/raw/endowment-manual/2026-09-07/` (virginiatech 2.09 NACUBO FY2025; kansasstate 1.0473 audited FY2025; dayton 0.919 official endowment page; wyoming 0.8574 audited FY2025 total-managed, scope-flagged incl. custodial; utahstate 0.64 Mar-2025 board memo "about $640 million", approximate; uvm 0.9725 FY2025 overview; macalester 0.92 facts sheet; wpi 0.7314 audited FY2025; usf 0.7228 audited FY2025); 140/200 endowment real (was 131/200), remaining 60 explicitly labeled `synthetic (placeholder)`; no score changes (endowment not in formula)
- v0.16 (2026-09-08): **DONE** — hbcusample identity cleanup: record displayed public name "HBCU Sample University" but carried scorecard_id 138947 = Clark Atlanta University, with baselines blended from another institution. Rematched to Clark Atlanta University (id renamed hbcusample→clarkatlanta; `_identity_source` annotated): name + control public→private (Scorecard ownership=2, UNCF), carnegie R1→R2 (CAU 2025 Carnegie announcement), enrollment_fte 33855→3603, grad_rate_6yr 0.72→0.4902 (both Scorecard real), retention 0.87→0.73 (CAU Office of Institutional Research; Scorecard retention field absent), sf_ratio 10→16 (College Board BigFuture 16:1, flagged for IPEDS-direct verification), research_spend_m 416→10.32 (CAU official: $10,320,000 FY2023), alumni_network_k 313→29 (UNCF "more than 29,000 alumni"), endowment_b 1.2 synthetic→0.116 (IPEDS Finance FY2024 ~$116M via Data USA, labeled non-NACUBO); endowment_per_student recomputed 32195; all 200 rescored (only CAU moved >0.05: 58.9→56.6). Raw artifacts: fresh Scorecard pull `data/raw/collegescorecard/2026-09-08/cau_138947_v16.json`, web-verified facts `data/raw/cau-rematch/2026-09-08/` (+ sources.json). 141/200 endowment real (was 140/200), 59 labeled synthetic placeholders. Data-quality flag: Scorecard `3_yr_default_rate` returned 0 for 138947 (implausible at 70% Pell — treated as suppressed; loan_default kept at 0.029 status quo, needs future audit)
- v0.17 (2026-09-09): **DONE** — identity/display-name correctness pass. (1) `oregon` identity rematch: record "University of Oregon" carried scorecard_id 209490 = Oregon Health & Science University (Portland), so earnings 101028, enrollment 836, debt 16625, net price 18000 were OHSU's. Rematched to 209551 = University of Oregon, Eugene (fresh Scorecard pull, raw artifact `data/raw/collegescorecard/2026-09-09/uo_209551_v17.json`, `_identity_source` annotated): earnings 61324, debt 20139, net price 22182, grad 0.7169, enrollment 20497, admission 0.883, pell 0.2135, family income 78050; retention field absent in pull — kept 0.87 status quo; `3_yr_default_rate` returned 0 (suppressed dataset-wide, same as CAU v0.16) — kept 0.039 placeholder, `loan_default_real` nulled; endowment_per_student recomputed 63424. All 200 rescored (only oregon moved >0.05: 78.5→64.4). (2) 20 malformed display names corrected (NYU University→New York University, UMD University→University of Maryland, Morehouse University→Morehouse College, Penn University→University of Pennsylvania, Case Western University→Case Western Reserve University, Tulane already correct; Scorecard formal names retained as source of truth). Validated: 200 records, 200/200 distinct ids/names/Scorecard IDs, valid JSON.
- v0.21 (2026-09-13): **DONE** — regression significance: every reported correlation now carries a two-tailed p-value (Student's t on Pearson r, t = r√(n−2)/√(1−r²), df=n−2). New JS stats toolkit in js/app.js (corrTest + fmtP + sigWord; Lanczos lgamma + Numerical Recipes betacf for the t CDF). Correlation insight card shows r, p, n, and "significant at α=0.05" verdicts for all three pairs (endow/earn r=0.63 p<0.001 n=200; earn/grad r=0.69 p<0.001; selectivity/score r=0.80 p<0.001 — all significant); endow/earn scatter annotates r and p top-right and in dot tooltips; methodology section documents the test. Validated: JS p-values match scipy.stats.pearsonr to printed precision on all three pairs + known t-table values (t=2.576/df=∞ → 0.009995; t=2.0/df=10 → 0.073388), node --check clean. No data change (0 score movers). Validated: 200 records, 200/200 distinct ids/Scorecard IDs, valid JSON.
- v0.22 (2026-09-14): **DONE** — sort state deep-links via ?sort=. Pure helpers parseSortParam/sortParamValue added to js/app.js (unit-tested in hidden_files/verify_sortlink_v22.js: parse table, URL-value table, round-trip over all 11 column keys x asc/desc — all pass). Column-header clicks now write ?sort=<key>_asc|desc (default score_desc removes the param); sort-preset select writes its preset value (score_desc/earn_desc/endow_desc/grad_desc/value_desc); init reads ?sort=, restores sortKey/sortDir + preset select, and re-applies filters so the first render honors it. Drive-by fix: clicking a column header while the sort preset was ROI/value previously did nothing (applyFilters early-returns on value_desc and ignored the click); sortBy now resets the preset to the neutral score_desc so column sorts take effect. Validated: 200 records, 200/200 distinct ids/Scorecard IDs, valid JSON, node --check clean, 0 score movers (metadata version bump only).
- v0.27 (2026-09-19): **DONE** — peer-benchmarking table CSV export. Closes the standing Interactivity issue #1 (no peer-benchmark export, flagged since v0.4 — ~20 runs) and the standing Analytical Depth issue #2 of the same name. New pure block (`BENCH-CSV-V27`) in js/app.js: `benchGroupKey` (conference→peer_group→Other fallback chain, carnegie/control/state passthrough), `groupBenchRows` (groups by mode, members sorted score desc, avgScore/avgEarn/avgROI with the same ROI math as the table, top-3 ids+names), `benchCsvCell` (RFC 4180 quoting), `buildBenchCSV` (header `<mode>_group,n,avg_score,avg_earn,avg_roi,top3`; raw unformatted values), `downloadBenchCSV` (filename `university-benchmark-<mode>.csv`, blob download). renderPeers now builds the benchmarking table FROM groupBenchRows (previously an inline duplicate of the same math) so the table and the export can never drift; 'Benchmark CSV' button next to the Peer Benchmarking heading; data persisted on `window.__lastBench`, handler on `window.__downloadBenchCSV`. Unit-tested in hidden_files/verify_benchcsv_v27.js (block mechanically extracted from app.js between markers, no transcription: key fallback chain, quoting incl. embedded quotes/commas/newlines, hand-computed avgScore/avgEarn/avgROI on a 6-school fixture, sort order, top3 ordering + id carry + 3-cap, carnegie-mode grouping, header names, exact Ivy row, no HTML/CR leaks, comma group-name quoting, 4-mode parity on the real 200-universe: n sums to 200 + sorted + sane, CSV line counts, download filename + blob content match, no-bench alert — 53/53 pass; one mid-run catch: expected unquoted top3 field initially asserted quoted — fixed expectation). Purely additive; 0 score movers (metadata version bump only). Validated: 200 records, 200/200 distinct ids/Scorecard IDs, valid JSON, node --check clean.
- v0.26 (2026-09-18): **DONE** — peer-network drag persistence. Closes the standing Interactivity issues #1 (no drag persistence) and #2 (drag fx cleared on end), flagged since v0.4 — ~20 runs. New pure block (`PEER-LAYOUT-V26`) in js/app.js: `peerLayoutHash` (djb2), `peerLayoutKey` (mode + sorted id set), `savePeerLayout` (normalizes x/w,y/h to [0,1], prefers pinned fx/fy), `loadPeerLayout` (strict id-set match — mode/filter drift means no restore, never misplaced pins; corrupt/missing JSON → null), `applySavedLayout` (scales to current canvas, clamps to bounds, pins fx/fy), `clearPeerLayout` (mode-prefixed key purge). renderPeers restores a saved layout for the exact mode + school set (stats line notes 'layout restored (N)'); dragend now KEEPS the pin (was: fx/fy cleared) and saves the layout (stats 'layout saved'); node tooltip notes positions are saved; a 'Reset layout' button next to the grouping select clears the mode's stored layouts and re-renders. Unit-tested in hidden_files/verify_peerlayout_v26.js (block mechanically extracted from app.js between markers, no transcription: key determinism/mode-sensitivity/order-insensitivity/drift-sensitivity, save→load round-trip, fx/fy priority, zero-w/null-storage guards, apply scaling/clamp/pin-count/unknown-skip, clear prefix scoping, save→restore simulation with pinned drag state — 32/32 pass; one mid-run catch: strict === on toFixed(4) round-trip float — relaxed to 0.1 tolerance). Purely additive; 0 score movers (metadata version bump only). Validated: 200 records, 200/200 distinct ids/Scorecard IDs, valid JSON, node --check clean.
- v0.25 (2026-09-17): **DONE** — compare-table CSV export. Closes the standing Interactivity issue #2 (no compare-table CSV export, flagged since v0.4 — ~20 runs). Extracted the compare cell renderer from `showCompare` into a marked pure block (`COMPARE-CSV-V25`): `COMPARE_DIMS`, `compareRaw` (raw values incl. roi_10yr math + numeric `_real` fallback), `compareCellHTML` (bit-identical formatting: fmtMoney / pct / toFixed(1) / real badges — verified against the old renderer in the unit test), `csvCell` (RFC 4180 quoting) and `buildCompareCSV` (header + 14 raw-value rows). Panel gets a green 'Download CSV' button next to Close; filename `university-compare-<ids>.csv`; picks persisted on `window.__lastCompare`, handler on `window.__downloadCompareCSV`. Unit-tested in hidden_files/verify_comparecsv_v25.js (block mechanically extracted from app.js between markers, no transcription: dims order, roi math, _real fallback, quoting incl. embedded quotes, raw values not formatted, null→empty, 15-line shape on the real 200-universe, no ●real badge leak into CSV, cellHTML smoke over all 200×14 — 29/29 pass). Purely additive; 0 score movers (metadata version bump only). Validated: 200 records, 200/200 distinct ids/Scorecard IDs, valid JSON, node --check clean.
- v0.24 (2026-09-16): **DONE** — Carnegie crosswalk edges in the peer network. Closes the standing Network Quality issue #1 (flagged since v0.4 — 18 runs). New pure helper `buildCrosswalkLinks(nodes,k)` in js/app.js: each school links to its k=2 score-nearest schools that share its Carnegie tier (R1/R2/Baccalaureate) but sit in a DIFFERENT conference — the conference <-> Carnegie crosswalk, drawn as amber edges (longer 70px link distance, weaker 0.1 strength) over the gray intra-group links. Crosswalk edges are hoverable (pair names, tier, both conferences, scores) with a top-right legend; stats line reports the crosswalk count (400 on the full 200). Unit-tested in hidden_files/verify_xwalk_v24.js (mechanically extracted from app.js between markers: crosswalk constraints, no self-links/dup pairs, per-node cap, score-nearest ordering, same-conf exclusion, missing-carnegie 'Other' bucketing, determinism, singleton tiers, real-200 volume — 42/42 pass). Purely additive; 0 score movers (metadata version bump only). Validated: 200 records, 200/200 distinct ids/Scorecard IDs, valid JSON, node --check clean.
- v0.33 (2026-09-25): **DONE** — HBCU/LAC cohort comparison. Closes the standing Analytical Depth issue "no HBCU/LAC cohort comparison insight" (flagged v0.31, v0.32) and the Interactivity "no v0.31-additions filter chip / no HBCU/LAC sort preset" gap. 21 HBCUs tagged in data (`hbcu: true` + per-record `_hbcu_source`; SWAC/MEAC/SIAC conference membership + Tennessee State + Hampton; scripts/tag_hbcu_v33.py). Rankings gains a cohort filter select (All Cohorts / HBCUs / Liberal Arts Colleges) with ?cohort= deep-link driving metrics, distributions, peer network, map, and table; two dynamic insight cards compare the HBCU cohort (n=21: avg score 58.0 vs 69.4 overall, top Howard 68.1) and the LAC cohort (n=43: avg score 72.0 vs R1 69.8, top Williams) with avg/median earnings, median ROI, and median endow/student. Detail panel provenance shows an HBCU chip. Pure helpers cohortPred/cohortStats/cohortROI (COHORT-V33 markers), unit-tested 28/28; all 9 suites green (318 total). Validated: 249 records, 249/249 distinct ids/Scorecard IDs, valid JSON, node --check clean, **0 score movers** (metadata + hbcu fields only, exact indent=2 diff).
- v0.34 (2026-09-26): **DONE** — synthetic-label honesty pass + alumni-giving feasibility probe closure. Closes the v0.18-style trust gap: the detail panel displayed Employment 6mo and Alumni Giving percentages with no provenance (research got this fix in v0.32). The 2026-09-26 probe concluded neither field is replaceable via a public API today: IPEDS Finance collects total private gifts with no alumni breakout; CASE VSE institution-level data is subscription-only (CASE member portal); US News publishes giving rates per profile page but has no public API; neither IPEDS nor Scorecard publishes 6-month employment rates (NACE first-destination surveys are per-school, not centralized). Probe recorded at data/raw/alumni-giving-probe/2026-09-26/README.md with re-probe triggers. Changes: per-record `_alumni_giving_source` / `_employment_6mo_source` = "synthetic (placeholder)" on all 249 (scripts/apply_synthetic_labels_v34.py, source field follows base field per convention); detail panel Outcomes block shows gray "(synthetic placeholder)" for both, mirroring researchLabel wording; methodology cards now read "estimated placeholder" instead of claiming "(IPEDS, CASE)" / "(IPEDS + surveys)". Unit-tested in hidden_files/verify_synthlabels_v34.js (21/21). Validated: 249 records, 249/249 distinct ids/Scorecard IDs, valid JSON, node --check clean, **0 score movers** (source-label + metadata fields only, exact indent=2 diff).
- v0.28 (2026-09-20): **DONE** — peer-network link strength by peer_group. Closes the standing Network Quality issue #1 (no link strength by peer_group, flagged since v0.5 — ~22 runs). New pure block (`PEER-LINK-V28`) in js/app.js: `peerLinkProps(a,b)` (dist = 28+min(52,1.4·|Δscore|), str = 0.38−min(0.24,0.009·|Δscore|); shared peer_group → −8 dist / +0.08 str, dist∈[20,80], str∈[0.14,0.5]) and `buildPeerLinks(nodes,k)` (each node links to its k=2 score-nearest group-mates, capped at top-12-by-score per group, pair-deduped, deterministic — replaces the old random 0.25 same-group loop). forceLink distance/strength now read the per-link values (crosswalk edges keep 70/0.1); intra-group link stroke-width varies with strength (0.55+str); stats line reports the weighted peer-link count; a second legend row explains the weighting. Unit-tested in hidden_files/verify_peerlinks_v28.js (block mechanically extracted from app.js between markers, no transcription: exact props incl. saturation caps + missing-peer_group guard, dist/str monotonicity in score gap, score-nearest ordering, no self-links/dup pairs, k cap, determinism, cap-12 participation, tie-break by id, id-string source/target shape, real-200 smoke: same-group, bounds, non-uniform weight — 36/36 pass; one mid-run catch: test expectation miscalc 0.19 vs correct 0.22 — fixed in the test). Purely additive; 0 score movers (metadata version bump only). Validated: 200 records, 200/200 distinct ids/Scorecard IDs, valid JSON, node --check clean.
- v0.32 (2026-09-24): **DONE** — close the new-49 placeholder debt with real data (one coherent data-quality improvement). (A) Endowment 249/249 real: 46 verified against Wikipedia infoboxes fetched 2026-09-24 (`data/raw/endowment-wikipedia/2026-09-24/`, all institution identities hand-adjudicated; 9 title mismatches caught in review and refetched by explicit title — Trinity CT Cambridge→Hartford $893.2M, U Delaware→Delaware State $43M, U South Carolina→SC State $17.2M, Annie Dillard→Dillard $100M, Connecticut list page→Connecticut College $510.9M, University and College Union→Union College $563.7M, Knox College Toronto→Galesburg, Southern redirect resolved to New Orleans private school). 3 primary-source values: Knox audited FY2024 $174.5M (knox.edu auditor's report), Morgan State $106M (Data USA IPEDS FY2024), Southern A&M $10.02M (CollegeDB IPEDS-derived). All 49 records carry per-value `_endowment_source`; endowment_per_student recomputed. (B) Research 235/249 real: 37/49 matched to NSF HERD FY2024 Table 19 via explicit allowlist against the already-committed parsed table (`data/raw/nsf-herd/2026-09-10/herd_table19_parsed.json`) — 10 near-miss traps avoided (Trinity CT→Hartford $5.1M not Trinity U. $5.8M, Delaware State not U Delaware $465.8M, Connecticut C. not UConn $433.6M, Lafayette C. not UL Lafayette, F&M not Marshall U., TN State not UT Knoxville, SC State not USC Columbia, NC Central not NC State, Southern Baton Rouge $5.5M with Ag Center $9.0M scope-noted, Lawrence Technological U. is a different school); 12 not in the HERD survey keep labeled `synthetic (placeholder)` (rhodes, centre, depauw, wabash, beloit, knox, holycross, bard, pitzer, grambling, bethunecookman, lawrence). Every record carries `_research_source` (+ `_research_herd_name`, `_research_rank`). (C) Label honesty: the detail panel's research line now shows "synthetic placeholder" for the 14 synthetic records instead of "NSF HERD" (the v0.18 trust bug, reintroduced for the new 49). Validated: 249 records, 249/249 distinct ids/Scorecard IDs, valid JSON, node --check clean, **0 score movers** (v0.10 formula uses neither endowment nor research), exact-2-line metadata diff convention re-established (indent=2 re-dump after serialization drift).
- v0.31 (2026-09-23): **DONE** — expand from 200 to 249 universities: 20 HBCUs + 30 liberal arts colleges (one, Claremont McKenna, was already in the 200 and deduped). All 49 new schools Scorecard-enriched via the College Scorecard API (249/249 real, 249/249 distinct IDs; earnings/debt/default/net-price/enrollment/admission/retention/Pell/family-income/grad-rate from Scorecard where available — one initial match failure, St. Olaf College, rematched to Scorecard 174844 'St Olaf College' via a follow-up pull). Existing 200 scores preserved exactly: new raw scores clamped into the fixed v0.10 raw range [70.74, 153.48] and mapped onto the same 55–97 scale (one clamp: Jackson State). Coordinates: 33/49 new schools from Scorecard location, 16 geocoded and labeled `location_source: "geocoded"` (Scorecard DEMO_KEY 429'd mid-pass — no more Scorecard calls this run). Endowment + HERD research for the new 49 were labeled synthetic placeholders at ship; both were verified real in v0.32 (endowment 249/249, research 235/249). UI/README/index badges → v0.31/250. Validated: 249 records, 249/249 distinct ids/Scorecard IDs, valid JSON, node --check clean, 0 score movers on the original 200.
- v0.30 (2026-09-22): **DONE** — geographic map draws peer-group links. Closes the standing Network Quality issue (map doesn't draw peer links, flagged since ~v0.5). New pure block (`MAP-PEERLINK-V30`) at module level in js/app.js (V26-V29 helpers are scoped inside renderPeers, so renderMap can't reach them): `mapLinkProps` (same weighting formula as v0.28 peerLinkProps with the same-peer_group bonus always active — map links are strictly peer_group-scoped: dist = 28+min(52,1.4·|Δscore|)−8, str = 0.38−min(0.24,0.009·|Δscore|)+0.08, str∈[0.22,0.46]), `mapPeerLinks(unis,k)` (groups by peer_group||'Other', top-12-by-score cap per group, k=2 score-nearest per node, pair-deduped, deterministic, carries full school records as a/b for projection), `escMapLink` + `mapPeerLinkTip` (pair, peer_group, scores with Δ, strength toFixed(3)). renderMap draws the links as thin #8a90aa lines (opacity 0.30, width 0.6+1.6·str) UNDER the campus dots, hoverable (brightens + tooltip, mouseout restores); a toggleable 'Peer links' checkbox (default on) re-renders the map; second legend row documents the encoding; geo-note reports the link count. Unit-tested in hidden_files/verify_mappeerlink_v30.js (block mechanically extracted from app.js between markers, no transcription: v0.28 formula parity incl. saturation caps, grouping/dedupe/k-cap/top-12 cap/determinism, tie-break by id, missing lat/lon/id exclusion, esc cases incl. no raw <script> leak, tip shape, real-200 smoke: 300 links all intra-peer_group, str bounds, no dup pairs, all endpoints have lon/lat, Big Ten capped at 12 participants, stroke widths sane — 38/38 pass; two mid-run catches both test-side: hand-made 'noid' fixture accidentally carried an id, and the k-cap assertion mis-modeled the deduped scheme — correct invariant is per-group pairs ≤ members·k). All prior verify suites still green. Purely additive; 0 score movers (metadata version bump only, exact 2-line JSON diff). Validated: 200 records, 200/200 distinct ids/Scorecard IDs, valid JSON, node --check clean.
- v0.29 (2026-09-21): **DONE** — peer-network intra-group link hover tooltips. Closes the standing Interactivity issue #1 and Network Quality issue #2 ("peer intra-group links not hoverable", flagged in the v0.28 panel — crosswalk edges have had hovers since v0.24, the gray peer links had none). New pure block (`PEER-LINKTIP-V29`) in js/app.js: `escTip` (HTML-escapes &, <, >, " — real names like "Texas A&M" and "William & Mary" contain &), `peerLinkTip(a,b,mode,link)` returning the tooltip HTML (pair names, shared mode-label + group, scores with score &#916;, link strength, same-peer_group bonus note). renderPeers wires mouseover/mousemove/mouseout on `link.filter(d=>!d.xwalk)`; mouseout restores the per-link strength width (0.55+str), NOT a constant — the width encodes link strength since v0.28. Legend row now notes "hover any link for the pair". Unit-tested in hidden_files/verify_peerlinktip_v29.js (block mechanically extracted from app.js between markers, no transcription: esc cases, names/arrow, mode labels for conference/carnegie/control/state + fallback, group fallback chain, missing scores/link -> ? / n/a, peer_group bonus gating, HTML escaping incl. no raw <script> leak, real-200 smoke — 38/38 pass). Purely additive; 0 score movers (metadata version bump only). Validated: 200 records, 200/200 distinct ids/Scorecard IDs, valid JSON, node --check clean.
- v0.23 (2026-09-15): **DONE** — dimension card rebuilt as a true radial radar chart. Killed the grouped-bar 'radar' and its /1.5 scale hack: 5 dimensions (Career = median_earn_10yr; Alumni = alumni_giving; Academic = grad_rate_6yr; Financial = log(endowment_per_student); Value = 1 − loan_default), each min–max normalized 0–100 with extents taken over the full 200 so the polygon shape stays comparable under filtering; polygons are private/public averages of the current filtered set (filtered-out side skipped, not zero-collapsed). New JS radar toolkit in js/app.js (RADAR_DIMS, radarExtents, radarNormVal, radarAverage, radarPoint — unit-tested in hidden_files/verify_radar_v23.js: 5 dims, extent/normalize/clamp/empty-guard cases, polar geometry — all pass): 25/50/75/100 grid rings, 5 spokes, axis labels, semi-transparent private (indigo) / public (green) polygons, vertex dots with hover tooltips showing per-dimension avg/100. index.html heading/desc, badge, footer, insights desc → v0.23. Validated: 200 records, 200/200 distinct ids/Scorecard IDs, valid JSON, node --check clean, 0 score movers (metadata version bump only).
- v0.18 (2026-09-10): **DONE** — research_spend_m goes real: 198/200 via NSF HERD FY2024 (nsf26304 Table 19, "all institutions ranked by all R&D expenditures"). Was 200/200 synthetic with no source annotations while the UI already displayed values labeled "NSF HERD" — a trust bug this run kills. Matcher: Jaccard token-set + 100+ hand-verified explicit aliases for known collisions (Miami FL vs Miami OH, USD vs UCSD, USFCA vs UCSF, Baylor U. vs Baylor College of Medicine, Morehouse C. vs School of Medicine, etc.); every one of 200 rows manually reviewed; caught and fixed 1 AUTO mismatch (purdue→Northwest 9.5, rematched to West Lafayette 931.5). Absurd legacy values corrected: morehouse 537→5.9, spelman 587→4.5, howard 472→101.8, usd 173→12.65, clarkatlanta 10.32→13.42 (HERD FY2024). Combined reporting units scope-flagged via `_research_scope` (pennstate University Park+Hershey, texasa&m College Station+Health Science Center, vanderbilt+medical center, dartmouth+Hitchcock, nebraska Lincoln+Medical Center). 2 records not in HERD survey keep labeled `synthetic (placeholder)`: gonzaga, xavier (Xavier U. Louisiana is a different school). Rank column in the sheet is by non-medical R&D — overall rank recomputed from totals. filings.herd→2024 for matched. Drive-by: 3 stale state fields fixed (wesleyan VA→CT Middletown, colby NH→ME Waterville, dayton FL→OH Dayton). Scores unchanged (v0.10 formula doesn't use research). Raw artifacts: `data/raw/nsf-herd/2026-09-10/` (xlsx + parsed JSON + envelope). Validated: 200 records, 200/200 distinct ids/Scorecard IDs, valid JSON.
- v0.20 (2026-09-12): **DONE** — geographic map: real lat/long for 200/200 via Wikipedia {{coord}} (MediaWiki prop=coordinates, primary first; 195/200 direct, 1 corrected page for hawaii athletics mismatch, 4 via Wikidata P625 where enwiki carries no {{coord}} at all: georgiatech Q864855, gwu Q432637, williamandmary Q875637, olemiss Q1138384). Full title-review pass: 3 REVIEW-LOW auto-picks verified correct, state-median outlier scan clean, 0 out-of-bounds. New "Geographic Map" section: D3 geoAlbersUsa + us-atlas states-10m topojson (CDN, graceful offline fallback), dots sized by score, indigo=private/green=public, click → detail + ?id= deep-link, hover tooltip, top-10 labels. lat/lon added to CSV export and detail-panel provenance. Raw artifacts: `data/raw/location-wikipedia/2026-09-12/`. Validated: 200 records, 0 score movers (location not in formula), valid JSON.
- v0.19 (2026-09-11): **DONE** — endowment gap fill round 3: all 59 remaining `synthetic (placeholder)` endowments replaced with values from each school's Wikipedia infobox (per-school fetch + full manual review; 6 search-result mismatches re-fetched on the correct page: washstate was UW $5.96B, usfca was UCSF $2.95B, und was NDSU $528M, ohiou was Wright State $120M, kentstate was University of Kent UK, uwseattle matched a campus page with no value). **200/200 endowment real, 0 synthetic remaining.** Absurd placeholders corrected: ucsb 3.8→0.6659, fresnostate 1.0→0.2548, uwseattle 2.7→5.96. Texas University Fund shares excluded for houston ($1.337B endowment, $1.35B TUF excluded) and unt ($356M, $1.18B TUF excluded); scope-flagged: hawaii system-wide $632.1M, umkc UMKC-only $204.14M (2023), uta/utdallas campus-only FY2024; akron value stale (as of 2020) labeled as such. endowment_per_student recomputed for all 59; all 200 rescored (0 movers — v0.10 formula doesn't use endowment). Raw artifacts: `data/raw/endowment-wikipedia/2026-09-11/` (infobox_extract.json, review.tsv, sources.json). Validated: 200 records, 200/200 distinct ids/Scorecard IDs, valid JSON.

- v0.5: Alumni network via LinkedIn alumni search (Fortune 500 %), startup founder enrichment (Crunchbase)
- v1.0: 500 universities, 6 filing sources 100% coverage, 5-critic panel 9.0+

## Iteration Loop

Same as S&P 500 tracker:

1. **Review** current site (index.html, css, js, data)
2. **Panel-evaluate** 5 critics: Data Richness, Visual Design, Interactivity, Network Quality, Analytical Depth
3. **Pick highest-impact fix**, implement in one run
4. **Commit & push** to `master`, GitHub Pages auto-deploys
5. **Log** to `hidden_files/iteration-log.md`

Cron: `university-outcomes-iteration` every 1h until avg 9.0+, then switch to 24h.

## Local Dev

```bash
cd ~/repos/university-outcomes-tracker
python3 -m http.server 8000
# open http://localhost:8000
```

## License

MIT — data from public sources, methodology documented.
